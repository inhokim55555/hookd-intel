# Copy this file to .env.local and fill in your values
# On Replit: add these in the Secrets tab (padlock icon) instead

GETHOOKD_API_KEY=your_gethookd_api_key_here
ANTHROPIC_API_KEY=sk-ant-your_anthropic_api_key_here

# Optional: add Replit PostgreSQL via the Database tool — DATABASE_URL is set automatically
# DATABASE_URL=postgresql://user:password@host:5432/dbname
